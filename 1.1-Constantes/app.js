const TAMANHO = 100;
const IGV = 0.18;

console.log(TAMANHO);
alert(IGV);

TAMANHO = 10;// Erro, pois não podemos alterar uma variável constante!

alert(TAMANHO); // Não será exibido o alertt por causa do erro a cima.